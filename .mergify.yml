queue_rules:
  - name: default
    conditions:
      - check-success='CI Start'

pull_request_rules:
  - name: merge using the merge queue
    conditions:
      - base=main
      - check-success='CI Start'
    actions:
      queue:
        name: default
